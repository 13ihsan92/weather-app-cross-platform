# Vue.JS (TypeScript Framework) + IONIC (WebView Multi Platform Framework)

## Installation

### 1. Install IONIC CLI

```bash
npm install -g @ionic/cli
```

### 2. Create Project

```bash
ionic start app blank --type vue
```

### 3. Debugging
```bash
ionic serve
```

### 4. Build
```bash
ionic build --prod
```